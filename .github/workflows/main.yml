name: Test Self-Hosted Runner
on:
  push:
    branches: [ main ]
jobs:
  test:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v4
    - name: Hello from Windows
      run: echo "✅ MY-PC runner работает!"
